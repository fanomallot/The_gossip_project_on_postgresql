<!DOCTYPE html>
<html>
	<head>
		<title>Modification des commentaires</title>
	</head>
	<body>
	<!-- affichage allert -->
	<% if flash[:success] %>
    <div class="alert alert-success" style="text-align: center;"><%= flash[:success]%></div>
    <% else  %>
    	<%if flash[:danger]%>
    <div class="alert alert-danger" style="text-align: center;"><%= flash[:danger]%></div>
    <% end %>
    <% end %>
    
<!-- affichage modif -->
	<%= form_tag url_for(action: 'update'), method: "put" do%>
  
   		<%= text_area_tag 'content' , @comment.content%><br/><br/>			
  		<%=submit_tag "save"%>
	<%end%></br>
<!-- affichage erreur -->
	<% if @comment.errors.any? %>
    	<p>Nous n'avons pas réussi à modifié le commentaire pour la (ou les) raison(s) suivante(s) :</p>
    	<ul>
     	<% @comment.errors.full_messages.each do |message| %>
     		<li><%= message %></li>
		<% end %>
		</ul>
	<% end %>
		</p>
<!-- retour en arrière -->
		<%=link_to 'retour à la page précedante',gossip_path(@comment.gossip.id)%></br>
		<%=link_to "retour à l'acceuil",gossips_path%>

	</body>
</html>